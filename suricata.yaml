vars:
  address-groups:
    HOME_NET: "[192.168.50.0/24]"
  interface: ens18

outputs:
  - fast.log:
      enabled: yes
  - eve-log:
      enabled: yes
      types:
        - alert:
        - anomaly:
        - dns:
        - http:
        - tls:
